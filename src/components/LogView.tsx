import React, {
  useCallback,
  useMemo,
  MouseEvent,
  useEffect,
  useRef,
} from 'react';
import { useDispatch } from 'react-redux';
// import { execEx, OnlyExec } from '../actions/RAction';

export const LogView: React.FC = () => {
  useEffect(() => {
    // OnlyExec();
  }, []);
  return (
    <>
      <p>now displaying analysis view</p>
    </>
  );
};
